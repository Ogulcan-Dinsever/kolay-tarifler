name: kolay_tarifler
description: "Kolay Tarifler — Türk ve dünya mutfaklarından tarif uygulaması"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # --- Firebase ---
  firebase_core: ^3.13.1
  firebase_auth: ^5.5.2
  cloud_firestore: ^5.6.6
  firebase_storage: ^12.4.5
  firebase_crashlytics: ^4.3.0
  firebase_messaging: ^15.2.5
  google_sign_in: ^6.2.2
  sign_in_with_apple: ^6.1.4 # App Store Guideline 4.8 — Google girişi varsa zorunlu
  crypto: ^3.0.3 # Apple girişi nonce hash'i için

  # --- Notifications ---
  flutter_local_notifications: ^18.0.1

  # --- State Management ---
  flutter_riverpod: ^2.6.1

  # --- Navigation ---
  go_router: ^14.8.1

  # --- UI ---
  cached_network_image: ^3.4.1
  table_calendar: ^3.1.3
  intl: ^0.20.2 # takvim Turkce ay/gun adlari (initializeDateFormatting)

  # --- Local Storage ---
  shared_preferences: ^2.5.3
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  google_fonts: ^8.0.2
  image_picker: ^1.1.2
  google_mobile_ads: ^9.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.3
  hive_generator: ^2.0.1
  build_runner: ^2.4.12

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/images/logo.png"
  adaptive_icon_background: "#F5F0EB"
  adaptive_icon_foreground: "assets/images/logo.png"
  min_sdk_android: 21

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/data/
