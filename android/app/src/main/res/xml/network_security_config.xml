<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Tüm HTTP trafiği engellendi; yalnızca HTTPS kabul edilir -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
